# Changelog

All notable changes to **scholar-rag-agent** are documented here.
Follows [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

## [Unreleased]

### Added
- `OrcidEducationConnector`: ORCID public education affiliations connector (`source_type=orcid_education`) with direct ORCID iD lookup and researcher profile search. See `docs/guides/ORCID_EDUCATION_SOURCE_GUIDE.md`. Prefer frontier models: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- `OpenAlexSourcesHostOrgConnector`: OpenAlex sources host-organization connector (`source_type=openalex_sources_host_org`) filtering venues by publisher/host organization id or name. See `docs/guides/OPENALEX_SOURCES_HOST_ORG_SOURCE_GUIDE.md`. Prefer frontier models: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- `CrossrefWorksIssnTypeConnector`: Crossref works-by-ISSN+type connector (`source_type=crossref_works_issn_type`) with `filter=issn:{issn},type:{type}` for structured queries and `has-issn:true` free-text search. See `docs/guides/CROSSREF_WORKS_ISSN_TYPE_SOURCE_GUIDE.md`. Prefer frontier models: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- DataCite DOIs-by-prefix connector (`datacite_dois_prefix`). See `docs/guides/DATACITE_DOIS_PREFIX_SOURCE_GUIDE.md`.
- Crossref works type+license connector (). See .
- `OpenAlexSourcesHierarchyConnector`: OpenAlex sources hierarchy connector (`source_type=openalex_sources_hierarchy`) normalizing host organization, source type, primary ISSN, and venue into an ancestry path. See `docs/guides/OPENALEX_SOURCES_HIERARCHY_SOURCE_GUIDE.md`. Prefer frontier models: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- `OrcidWorksSummariesConnector`: ORCID works summaries connector (`source_type=orcid_works_summaries`) for ORCID-iD-first public work summaries with external ids. See `docs/guides/ORCID_WORKS_SUMMARIES_SOURCE_GUIDE.md`. Prefer frontier models: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- `OpenAlexConceptsAncestorsConnector`: OpenAlex concepts ancestors hierarchy connector (`source_type=openalex_concepts_ancestors`) normalizing `ancestors` into `ancestor_path`. See `docs/guides/OPENALEX_CONCEPTS_ANCESTORS_SOURCE_GUIDE.md`. Prefer frontier models: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- `CrossrefWorksLicenseConnector`: Crossref works-by-license connector (`source_type=crossref_works_license`) with `filter=license.url:{url}` for license URLs and `has-license:true` free-text search. See `docs/guides/CROSSREF_WORKS_LICENSE_SOURCE_GUIDE.md`. Prefer frontier models: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- `OpenAlexAuthorWorksConnector`: OpenAlex author works citations-blend connector (`source_type=openalex_author_works`) resolving author ids/names then `GET /works?filter=authorships.author.id:{id}` with `cited_by_count`. See `docs/guides/OPENALEX_AUTHOR_WORKS_SOURCE_GUIDE.md`. Prefer frontier models: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- `DataCiteReportsConnector`: DataCite reports DOI connector (`source_type=datacite_reports`) via `GET /dois?query=...&resource-type-id=report`. See `docs/guides/DATACITE_REPORTS_SOURCE_GUIDE.md`. Prefer frontier models: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- `CrossrefWorksFunderConnector`: Crossref works-by-funder connector (`source_type=crossref_works_funder`) with `filter=funder:{id}` for Open Funder Registry ids and `has-funder:true` free-text search. See `docs/guides/CROSSREF_WORKS_FUNDER_SOURCE_GUIDE.md`. Prefer frontier models: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- `EuropePmcPreprintsConnector`: Europe PMC preprint-only search connector (`source_type=europepmc_preprints`) with an enforced `SRC:PPR` filter. See `docs/guides/EUROPEPMC_PREPRINTS_SOURCE_GUIDE.md`. Prefer frontier models: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- `OrcidEmploymentsConnector`: ORCID public employments/affiliations connector (`source_type=orcid_employments`) with direct ORCID iD lookup and researcher profile search. See `docs/guides/ORCID_EMPLOYMENTS_SOURCE_GUIDE.md`. Prefer frontier models: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- `DataCiteEventsConnector`: DataCite Event Data connector (`source_type=datacite_events`) for DOI citation, usage, and relationship events. See `docs/guides/DATACITE_EVENTS_SOURCE_GUIDE.md`. Prefer frontier models: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- `CrossrefJournalsConnector`: Crossref journals API connector (`source_type=crossref_journals`) with free-text search and ISSN resolve. See `docs/guides/CROSSREF_JOURNALS_SOURCE_GUIDE.md`. Prefer frontier models: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- `EuropePmcGrantsConnector`: Europe PMC GRIST grants connector (`source_type=europepmc_grants`). See `docs/guides/EUROPEPMC_GRANTS_SOURCE_GUIDE.md`. Prefer frontier models: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- `OpenAlexKeywordsConnector`: OpenAlex keywords API connector (`source_type=openalex_keywords`) with free-text search and keyword URL / `keywords/{slug}` resolve. See `docs/guides/OPENALEX_KEYWORDS_SOURCE_GUIDE.md`. Prefer frontier models: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- `CrossrefTypesConnector`: Crossref works type-filter connector (`source_type=crossref_types`). See `docs/guides/CROSSREF_TYPES_SOURCE_GUIDE.md`. Prefer frontier models: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- `SemanticScholarBulkConnector`: Semantic Scholar Graph `/paper/batch` connector (`source_type=semantic_scholar_bulk`). See `docs/guides/SEMANTIC_SCHOLAR_BULK_SOURCE_GUIDE.md`. Prefer frontier models: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- `OpenAlexTopicsHierarchyConnector`: OpenAlex topics hierarchy connector (`source_type=openalex_topics_hierarchy`). See `docs/guides/OPENALEX_TOPICS_HIERARCHY_SOURCE_GUIDE.md`. Prefer frontier models: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- `OpenAlexFundersConnector`: OpenAlex funders API connector (`source_type=openalex_funders`) with free-text search and `F####` / URL resolve. See `docs/guides/OPENALEX_FUNDERS_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- `PubmedMeshConnector`: NCBI MeSH vocabulary connector via E-utilities `esearch`/`esummary` (`db=mesh`, `source_type=pubmed_mesh`) with UI, preferred name, and tree numbers in metadata. See `docs/guides/PUBMED_MESH_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- `ArxivHtmlAbstractConnector`: arXiv abs HTML abstract enrichment connector (`source_type=arxiv_html_abstract`) that fetches `https://arxiv.org/abs/{id}` abstract text (or enriches free-text arXiv API hits). See `docs/guides/ARXIV_HTML_ABSTRACT_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- `OpenAlexPublishersConnector`: OpenAlex publishers API connector (`source_type=openalex_publishers`) with free-text search and `P####` id resolve. See `docs/guides/OPENALEX_PUBLISHERS_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- `OrcidWorksFilterConnector`: ORCID works year/type deep-filter (`source_type=orcid_works_filter`). See `docs/guides/ORCID_WORKS_FILTER_SOURCE_GUIDE.md`.
- `CrossrefRelationsConnector`: Crossref works relation-types enrichment (`source_type=crossref_relations`). See `docs/guides/CROSSREF_RELATIONS_SOURCE_GUIDE.md`.
- `OpenAlexSourcesConnector`: OpenAlex sources/venues API connector (`source_type=openalex_sources`) with free-text search and `S####` id resolve. See `docs/guides/OPENALEX_SOURCES_SOURCE_GUIDE.md`.
- DataCite related-identifier enrichment ingestion connector (`ingestion/datacite_related.py`) that queries `GET https://api.datacite.org/dois?query=...` and normalizes each JSON:API DOI resource while enriching `relatedIdentifiers` from attributes (`relationType`, `relatedIdentifierType`, `relatedIdentifier`) into searchable text and `metadata.related_identifiers` with `metadata.source_type="datacite_related"`. Distinct from `datacite.py`, which focuses on core bibliographic metadata. Blank and non-positive searches short-circuit without HTTP; unavailable DataCite responses yield an empty list. See `docs/guides/DATACITE_RELATED_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- OpenAIRE projects ingestion connector (`ingestion/openaire_projects.py`) that queries `GET https://api.openaire.eu/search/projects?format=json` with `keywords` (or `name` for title-shaped queries, `grantID` / `openaireProjectID` for identifier lookups) and normalizes each project (`title`, `summary` abstract, `code`, `originalId`, `keywords`, funder name, start/end dates, `fundedamount`) into `Document`s with `metadata.source_type="openaire_projects"`. Projects-only — distinct from `openaire.py` research products. Blank and non-positive searches short-circuit without HTTP; unavailable OpenAIRE responses yield an empty list. See `docs/guides/OPENAIRE_PROJECTS_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- OpenAlex institutions ingestion connector (`ingestion/openalex_institutions.py`) that queries `GET https://api.openalex.org/institutions` with a free-text `search` query (or resolves bare / URL OpenAlex institution ids via `GET /institutions/{id}`) and normalizes each institution (`display_name`, `type`, `country_code`, `works_count`, `cited_by_count`, ROR and Wikidata links, `geo` city/country, `summary_stats` h-index) into `Document`s with `metadata.source_type="openalex_institutions"`. Blank and non-positive searches short-circuit without HTTP; unavailable OpenAlex responses yield an empty list. See `docs/guides/OPENALEX_INSTITUTIONS_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- OpenAlex concepts ingestion connector (`ingestion/openalex_concepts.py`) that queries `GET https://api.openalex.org/concepts` with a free-text `search` query (or resolves bare / URL OpenAlex concept ids via `GET /concepts/{id}`) and normalizes each concept (`display_name`, `description`, `works_count`, `cited_by_count`, `level`, Wikidata link) into `Document`s with `metadata.source_type="openalex_concepts"`. Concept-shaped queries (`C####`) resolve the concept directly and sample representative works via `GET https://api.openalex.org/works?filter=concepts.id:{id}`. Blank and non-positive searches short-circuit without HTTP; unavailable OpenAlex responses yield an empty list. See `docs/guides/OPENALEX_CONCEPTS_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Crossref members ingestion connector (`ingestion/crossref_members.py`) that queries `GET https://api.crossref.org/members` with a free-text `query` (or resolves bare numeric member ids via `GET /members/{id}`) and normalizes each member (`primary-name`, `location`, DOI `prefixes`, alternate `names`, `counts.total-dois`) into `Document`s with `metadata.source_type="crossref_members"`. Blank and non-positive searches short-circuit without HTTP; unavailable Crossref responses yield an empty list. See `docs/guides/CROSSREF_MEMBERS_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- SSRN preprint DOI bridge ingestion connector (`ingestion/ssrn.py`) that queries `GET https://api.crossref.org/works` with a free-text `query` plus `filter=prefix:10.2139` (or resolves SSRN-shaped DOIs via `GET /works/{doi}`) and normalizes each preprint (`title`, JATS-stripped `abstract`, `author`, publication year, `container-title`, SSRN landing URL) into `Document`s with `metadata.source_type="ssrn"` and stable `document_id` via `stable_id`. Blank and non-positive searches short-circuit without HTTP; unavailable Crossref responses yield an empty list. See `docs/guides/SSRN_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- OpenAlex authors ingestion connector (`ingestion/openalex_authors.py`) that queries `GET https://api.openalex.org/authors` with a free-text `search` query (or resolves bare / URL OpenAlex author ids via `GET /authors/{id}`) and normalizes each author (`display_name`, ORCID, `works_count`, `cited_by_count`, primary `last_known_institutions` affiliation, `summary_stats` h-index / i10-index) into `Document`s with `metadata.source_type="openalex_authors"`. Blank and non-positive searches short-circuit without HTTP; unavailable OpenAlex responses yield an empty list. See `docs/guides/OPENALEX_AUTHORS_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- bioRxiv / medRxiv collections ingestion connector (`ingestion/biorxiv_collections.py`) that queries the public bioRxiv details API `collection` response via category-filtered date-range requests (`?category=...`), recent-post windows, or DOI detail lookups, and normalizes each preprint (`title`, `abstract`, `doi`, `date` year, `authors`, `category`, `server`) into `Document`s with `metadata.source_type="biorxiv_collections"`. Blank and non-positive searches short-circuit without HTTP. See `docs/guides/BIORXIV_COLLECTIONS_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Wikidata scholarly ingestion connector (`ingestion/wikidata_scholarly.py`) that searches the public Wikidata Action API via `wbsearchentities`, enriches hits with `wbgetentities` (labels, descriptions, DOI/publication-date claims, sitelinks), and falls back to a lightweight SPARQL query on `https://query-scholarly.wikidata.org/sparql` when entity search returns no usable matches. Each hit is normalized into a `Document` with `metadata.source_type="wikidata_scholarly"`. QID-shaped queries resolve directly. Blank and non-positive searches short-circuit without HTTP; unavailable API responses yield an empty list. See `docs/guides/WIKIDATA_SCHOLARLY_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- ClinicalTrials.gov ingestion connector (`ingestion/clinicaltrials.py`) that queries the public ClinicalTrials.gov API v2 `studies` endpoint by keyword (`query.term`) and normalizes each study (`briefTitle`/`officialTitle`, `briefSummary`, NCT ID, overall status, start-date year, conditions, study type/phases, lead sponsor) into a `Document` with `metadata.source_type="clinicaltrials"`. Blank and non-positive searches short-circuit without HTTP. See `docs/guides/CLINICALTRIALS_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Crossref Funder Registry ingestion connector (`ingestion/crossref_funder.py`) that queries `GET https://api.crossref.org/funders` with a free-text `query` (or resolves bare / `10.13039/...` funder ids via `GET /funders/{id}`) and normalizes each funder (`name`, `alt-names`, `location`, Open Funder Registry `id` / `uri`, optional `work-count` / `descendant-work-count`) into `Document`s with `metadata.source_type="crossref_funder"`. Blank and non-positive searches short-circuit without HTTP; unavailable Crossref responses yield an empty list. See `docs/guides/CROSSREF_FUNDER_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- PMC Open Access package connector (`ingestion/pmc_oa.py`) that extracts PMCIDs from free text, queries `GET https://www.ncbi.nlm.nih.gov/pmc/utils/oa/oa.fcgi?id={pmcid}`, and normalizes OA package/PDF download links (`citation`, license, retracted flag, HTTPS `package_url` / `pdf_url`, formats) into `Document`s with `metadata.source_type="pmc_oa"`. Complements E-utilities `PmcConnector` full-text XML search with package link discovery for download hooks. Blank, non-PMCID, and non-positive searches short-circuit without HTTP; missing IDs and HTTP failures are skipped. See `docs/guides/PMC_OA_PACKAGE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Dryad ingestion connector (`ingestion/dryad.py`) that queries the public Dryad REST `search` endpoint by keyword (`q`) and normalizes each embedded dataset (`title`, HTML-stripped `abstract`, `authors`, `publicationDate` year, `identifier` DOI, `fieldOfScience`, `sharingLink`) into a `Document` with `metadata.source_type="dryad"`. Blank and non-positive searches short-circuit without HTTP. See `docs/guides/DRYAD_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- OpenAlex topics ingestion connector (`ingestion/openalex_topics.py`) that queries `GET https://api.openalex.org/topics` with a free-text `search` query and normalizes each topic (`display_name`, `description`, `works_count`, subfield/field/domain taxonomy) into `Document`s with `metadata.source_type="openalex_topics"`. Topic-shaped queries (`T####`) resolve the topic directly and sample representative works via `GET https://api.openalex.org/works?filter=topics.id:T####`. Blank and non-positive searches short-circuit without HTTP; unavailable OpenAlex responses yield an empty list. See `docs/guides/OPENALEX_TOPICS_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Crossref Event Data ingestion connector (`ingestion/crossref_events.py`) that queries `GET https://api.eventdata.crossref.org/v1/events` with `rows` plus either `query.bibliographic` for free-text bibliographic queries or `obj-id` when the query is DOI-shaped, and normalizes each event (`subj`/`obj` metadata, relation type, source id, timestamps, evidence record, object DOI) into `Document`s with `metadata.source_type="crossref_events"`. Blank and non-positive searches short-circuit without HTTP; unavailable Event Data responses yield an empty list. See `docs/guides/CROSSREF_EVENTS_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Retraction-aware OpenAlex ingestion connector (`ingestion/retraction_watch.py`) that searches `GET https://api.openalex.org/works` with `filter=is_retracted:true` plus a free-text `search` query, keeps only explicitly retracted works, and normalizes title, reconstructed abstract (or a retraction descriptor), DOI, authors, year, journal, OpenAlex id, citation count, and landing URL into `Document`s with `metadata.source_type="retraction_watch"` and `metadata.is_retracted="true"`. Blank and non-positive searches short-circuit without HTTP; unavailable OpenAlex responses yield an empty list. See `docs/guides/RETRACTION_CHECK_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Unpaywall ingestion connector (`ingestion/unpaywall.py`) that extracts DOI identifiers from a DOI-like query or free text, fetches `GET https://api.unpaywall.org/v2/{doi}?email=...`, and normalizes OA landing-page/PDF metadata (`title`, authors, year, journal, OA status, best OA location, host type, version, license) into `Document`s with `metadata.source_type="unpaywall"`. The API requires a contact email via constructor or `UNPAYWALL_EMAIL`; blank, non-DOI, non-positive, and missing-email searches short-circuit without HTTP. See `docs/guides/UNPAYWALL_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Semantic Scholar recommendations support on `SemanticScholarConnector.recommendations()`, which expands a seed Semantic Scholar paper id or supported external id such as `DOI:...` through `GET https://api.semanticscholar.org/recommendations/v1/papers/forpaper/{seed}` and normalizes each `recommendedPapers[]` entry into a `Document` with `metadata.source_type="semantic_scholar_recommendations"`, seed provenance, Semantic Scholar id, DOI, authors, and year. The connector uses `httpx.AsyncClient`, URL-encodes DOI seeds, and caps each recommendations request conservatively at 20 results. See `docs/guides/SEMANTIC_SCHOLAR_RECOMMENDATIONS_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- OpenCitations ingestion connector (`ingestion/opencitations.py`) that extracts DOI identifiers from a free-text query or DOI list, fetches OpenCitations Meta metadata via `GET https://api.opencitations.net/meta/v1/metadata/doi:{doi}`, and enriches each record with best-effort Index v2 `citation-count` / `reference-count` values from `https://api.opencitations.net/index/v2`. It normalizes title, authors, publication date/year, venue, type, DOI, identifiers, and counts into a `Document` with `metadata.source_type="opencitations"`. The probed Meta `/search` routes returned 404, so this connector is intentionally DOI-centric. See `docs/guides/OPENCITATIONS_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 2.5**, **Kimi K2**.
- OSF ingestion connector (`ingestion/osf.py`) that queries the public Open Science Framework JSON:API `preprints` and `registrations` endpoints by keyword (`filter[search]`) and normalizes each JSON:API record (`title`, `description`, published/registered year, DOI, embedded contributors, category, OSF id, landing link) into a `Document`. It adds a seventeenth scholarly source focused on open-science preprints and registered study records alongside PDF, arXiv, Semantic Scholar, OpenAlex, PubMed, Crossref, Europe PMC, DOAJ, DBLP, HAL, OpenAIRE, Zenodo, Figshare, CORE, bioRxiv/medRxiv, NASA ADS, and DataCite. Missing or unavailable OSF API responses are handled gracefully as empty endpoint results. See `docs/guides/OSF_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 2.5**, **Kimi K2**.
- ORCID Public API ingestion connector (`ingestion/orcid.py`) that searches public ORCID records by keyword via `expanded-search`, fetches matching records' public `works`, filters work summaries by query tokens, and also resolves bare or URL ORCID iD queries directly against `{orcid}/works`. It normalizes ORCID work summaries (`title`, `publication-date` year, DOI external IDs, journal title, work type, put-code, profile name / ORCID iD) into `Document`s with searchable descriptor text because ORCID does not expose abstracts. It adds a seventeenth scholarly source focused on author-curated public records alongside PDF, arXiv, Semantic Scholar, OpenAlex, PubMed, Crossref, Europe PMC, DOAJ, DBLP, HAL, OpenAIRE, Zenodo, Figshare, CORE, bioRxiv/medRxiv, NASA ADS, and DataCite. See `docs/guides/ORCID_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 2.5**, **Kimi K2**.
- bioRxiv / medRxiv ingestion connector (`ingestion/biorxiv.py`) that queries the public Cold Spring Harbor Laboratory details API for recent posts (or a DOI lookup) on `biorxiv` or `medrxiv`, filters client-side by query tokens against title/abstract/category, and normalizes each match (`title`, `abstract`, `doi`, `date` year, `authors`, `category`) into a `Document`. It adds a fifteenth scholarly source focused on life-science and clinical preprints alongside PDF, arXiv, Semantic Scholar, OpenAlex, PubMed, Crossref, Europe PMC, DOAJ, DBLP, HAL, OpenAIRE, Zenodo, Figshare, and CORE. Semantic Scholar now also exposes `search()` against the Graph `paper/search` endpoint. See `docs/guides/BIORXIV_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 2.5**, **Kimi K2**.
- NASA ADS ingestion connector (`ingestion/ads.py`) that queries the NASA Astrophysics Data System REST `search/query` endpoint by keyword (`q`) and normalizes each hit (`title`, `abstract`, `author`, `year`, `doi`, `bibcode`, `pub`) into a `Document`. It adds a sixteenth scholarly source focused on astronomy and astrophysics literature alongside PDF, arXiv, Semantic Scholar, OpenAlex, PubMed, Crossref, Europe PMC, DOAJ, DBLP, HAL, OpenAIRE, Zenodo, Figshare, and CORE. A concise author/venue/year descriptor is synthesized when a record carries no abstract. An optional `ADS_API_TOKEN` (or constructor `api_key`) is forwarded as a Bearer token; missing tokens return an empty list without calling the API. Semantic Scholar now also exposes `search()` against the Graph `paper/search` endpoint. See `docs/guides/ADS_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 2.5**, **Kimi K2**.
- DataCite ingestion connector (`ingestion/datacite.py`) that queries the DataCite public REST `dois` endpoint by keyword (`query`) and normalizes each JSON:API DOI resource (`titles`, `creators`, Abstract `descriptions`, `publicationYear`, `publisher`, `url`, `types.resourceTypeGeneral`) into a `Document`. It adds a sixteenth scholarly source focused on research-data and software DOIs alongside PDF, arXiv, Semantic Scholar, OpenAlex, PubMed, Crossref, Europe PMC, DOAJ, DBLP, HAL, OpenAIRE, Zenodo, Figshare, CORE, and bioRxiv/medRxiv. A concise author/publisher/year descriptor is synthesized when a record carries no description. See `docs/guides/DATACITE_SOURCE_GUIDE.md`. Prefer frontier models for downstream synthesis: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 2.5**, **Kimi K2**.
- CORE ingestion connector (`ingestion/core.py`) that queries the CORE public REST `search/works` endpoint by keyword (`q`) and normalizes each work (`title`, `abstract`, `yearPublished`, `doi`, author `name`s, `links` display landing page / `downloadUrl`) into a `Document`. It adds a fourteenth scholarly source and one of the largest open-access full-text indexes, alongside PDF, arXiv, Semantic Scholar, OpenAlex, PubMed, Crossref, Europe PMC, DOAJ, DBLP, HAL, OpenAIRE, Zenodo, and Figshare. A concise author/year descriptor is synthesized as the document text when a work carries no abstract. An optional API key is forwarded as a Bearer token. See `docs/guides/CORE_SOURCE_GUIDE.md`.
- Figshare ingestion connector (`ingestion/figshare.py`) that queries the Figshare public REST `articles/search` endpoint by keyword (`search_for`) and normalizes each article (`title`, HTML-stripped `description` when present, `published_date` year, `doi`, `url_public_html` landing page) into a `Document`. It adds a thirteenth scholarly source and another large general-purpose open-research repository with strong coverage of figures, datasets, media, posters, theses, and code, alongside PDF, arXiv, Semantic Scholar, OpenAlex, PubMed, Crossref, Europe PMC, DOAJ, DBLP, HAL, OpenAIRE, and Zenodo. The HTML description is reduced to plain text, and a concise year descriptor is synthesized as the document text when an article carries no description.
- Zenodo ingestion connector (`ingestion/zenodo.py`) that queries the Zenodo (CERN) public REST `records` endpoint by keyword and normalizes each record (`metadata.title`, creator `name`s, HTML-stripped `description`, `publication_date` year, DOI from the top-level or metadata field, `links.html` landing page) into a `Document`. It adds a twelfth scholarly source and a large general-purpose open-science repository with strong coverage of long-tail, self-deposited, and non-traditional outputs (datasets, software, preprints), alongside PDF, arXiv, Semantic Scholar, OpenAlex, PubMed, Crossref, Europe PMC, DOAJ, DBLP, HAL, and OpenAIRE. The HTML description is reduced to plain text, and a concise author/year descriptor is synthesized as the document text when a record carries no description.
- OpenAIRE ingestion connector (`ingestion/openaire.py`) that queries the OpenAIRE Graph public `researchProducts` endpoint by keyword and normalizes each research product (`mainTitle`, author `fullName`s, `descriptions` abstract, `publicationDate` year, DOI from typed `pids`, open-access landing URL from `instances[].urls`) into a `Document`. It adds an eleventh scholarly source and a very large cross-disciplinary open-science graph with strong EU-funded and institutionally deposited coverage, alongside PDF, arXiv, Semantic Scholar, OpenAlex, PubMed, Crossref, Europe PMC, DOAJ, DBLP, and HAL. A concise author/year descriptor is synthesized as the document text when a record carries no abstract.
- HAL ingestion connector (`ingestion/hal.py`) that queries the HAL (CCSD/CNRS) public Solr-backed `search` endpoint by keyword and normalizes each publication (title, full author names, abstract, landing-page URI, DOI, year) into a `Document`. It adds a tenth scholarly source and France's multidisciplinary open archive — with especially strong European mathematics, physics, and CS coverage — alongside PDF, arXiv, Semantic Scholar, OpenAlex, PubMed, Crossref, Europe PMC, DOAJ, and DBLP. Solr multi-valued fields (arrays even when single-valued) are normalized, and a concise author/year descriptor is synthesized as the document text when a record carries no abstract.
- DBLP ingestion connector (`ingestion/dblp.py`) that queries the DBLP public `search/publ` endpoint by keyword and normalizes each publication (title, authors, venue, year, DOI, electronic-edition link) into a `Document`. It adds a ninth scholarly source and the first dedicated computer-science bibliography alongside PDF, arXiv, Semantic Scholar, OpenAlex, PubMed, Crossref, Europe PMC, and DOAJ. As DBLP is a metadata index without abstracts, a concise author/venue/year descriptor is synthesized as the document text so sparse and entity retrieval retain real signal.
- DOAJ ingestion connector (`ingestion/doaj.py`) that queries the DOAJ public `search/articles` endpoint by keyword and normalizes each `bibjson` article (title, abstract, DOI, year, full-text link) into a `Document`. It adds an eighth scholarly source that guarantees a freely readable open access full text alongside PDF, arXiv, Semantic Scholar, OpenAlex, PubMed, Crossref, and Europe PMC.

### Fixed
- DataCite now accepts integer-valued float publication years (`2024.0` and `"2024.0"`) instead of dropping the year when upstream JSON serializes `publicationYear` as a float-like value.
- CORE `yearPublished` parsing now accepts date-shaped strings such as `2021-07-01` by extracting the leading four-digit year instead of dropping the year metadata.
- Semantic Scholar publication year used a naive coercion of the `year` field, so a JSON `null` could surface as the literal string `"None"` in metadata and a usable `publicationDate` (e.g. `2023-05-01`) was ignored when `year` was absent. The year is now resolved None-aware from an integer/digit `year`, falling back to the leading four digits of `publicationDate`, else `""`.
- Figshare and Zenodo publication years were taken as the first four characters of `published_date` / `publication_date` unconditionally, so free-text placeholders such as `unpublished` leaked garbage (for example `unpu`) into `metadata['year']`. The year is now accepted only when the date string matches `^\d{4}`.
- PubMed publication year was read only from `PubDate/Year`, which many records (seasonal issues, date ranges) omit while still carrying a `MedlineDate` such as `2024 Spring`; the year was silently dropped for those records. The year is now resolved from `Year`, falling back to the leading four characters of `MedlineDate`.
- Europe PMC publication year was read only from `pubYear`, which some records (notably preprints and ahead-of-print articles) omit while still carrying a full `firstPublicationDate`; the year was silently dropped for those records. The year is now resolved from `pubYear`, falling back to the 4-digit prefix of `firstPublicationDate`.
- DBLP connector dropped list-valued `venue` and `ee` fields. DBLP collapses a single value to a scalar but returns a *list* when a record carries several (multiple venues or electronic editions); `_as_str` coerced such lists to an empty string, so the venue was lost from the metadata and descriptor and the source URL fell off the preferred electronic edition onto a weaker fallback. `_as_str` now reads the first coercible element of a list.
- Crossref publication year was read only from the unified `published` date field, which Crossref does not always populate; the year was silently dropped for the many records that carry it solely under `issued` (Crossref's canonical publication date). The year is now resolved from `published`, then `issued`, then `published-print`/`published-online`.
- Dense hashing embeddings tokenized on raw whitespace while the BM25 sparse retriever strips surrounding punctuation, so a term such as `retrieval.` embedded into a different dimension than `retrieval` and the same word was a hit in sparse retrieval but a miss in the dense vector the two are fused with. The embedder now uses the shared `retrieval.sparse.tokenize` helper, making dense and sparse retrieval bucket a term identically.
- Crossref abstracts left XML/HTML entities undecoded: `_strip_jats` removed JATS tags but not entity references, so `&lt;`, `&amp;`, and numeric character references such as `&#945;` (α) leaked into the stored abstract as raw markup. Entities are now decoded via `html.unescape`, yielding readable, searchable prose.

### Added (earlier this cycle)
- Europe PMC ingestion connector (`ingestion/europepmc.py`) that queries the Europe PMC REST `search` endpoint (`resultType=core`) by keyword and normalizes each result (title, abstract, DOI, year, PMID) into a `Document`. It adds a seventh scholarly source that federates PubMed/MEDLINE, PubMed Central, preprints, and patents alongside PDF, arXiv, Semantic Scholar, OpenAlex, PubMed, and Crossref.
- Crossref ingestion connector (`ingestion/crossref.py`) that queries the Crossref REST `works` endpoint by keyword and normalizes each work (title, JATS-stripped abstract, DOI, year) into a `Document`. It adds the largest cross-disciplinary DOI index as a sixth scholarly source alongside PDF, arXiv, Semantic Scholar, OpenAlex, and PubMed.
- PubMed ingestion connector (`ingestion/pubmed.py`) that runs the NCBI E-utilities `esearch`+`efetch` flow to resolve a free-text query to PMIDs and normalize each article (title, structured multi-section abstract, PMID, year) into a `Document`. It is the first keyword-search connector, so one call can ingest several biomedical papers for a topic.
- OpenAlex ingestion connector (`ingestion/openalex.py`) that fetches works from the OpenAlex API and reconstructs the abstract from its `abstract_inverted_index` representation, adding a fourth open scholarly source alongside PDF, arXiv, and Semantic Scholar.
- Maximal Marginal Relevance (MMR) diversity re-ranker (`retrieval/mmr.py`) that reduces near-duplicate chunks by balancing query relevance against novelty (dependency-free lexical Jaccard similarity); available as an optional, default-off `HybridRetriever` stage.
- Public repository scaffold with Python 3.11+ packaging, CI, Docker, docs, and tests.
- Observe -> Decide -> Act agent runtime with an explicit persisted state machine.
- SQLite event log for state transitions and decision traces.
- PDF, arXiv, and Semantic Scholar ingestion connectors.
- Dense, sparse, HyDE, RRF, GraphRAG, multi-hop, and reranking retrieval components.
- LLM adapter contracts and optional provider adapters for OpenAI, Anthropic, Gemini, and Kimi.
- Citation grounding and `[UNGROUNDED]` hallucination guard.
- FastAPI endpoints for health, text ingestion, query execution, and run events.
- Demo GIF gallery and deterministic local demo scripts.

### Changed
- README restored to project-specific use cases, demos, architecture, and setup instructions.
- CI matrix aligned with the Python 3.11+ package requirement.
- Utility scripts and docs corrected to avoid generated placeholder APIs.

### Fixed
- PubMed connector no longer truncates abstracts that contain inline formatting elements (for example `<i>` around gene names or `<sup>` for exponents). The previous `node.text` read captured only the run of text before the first inline child, silently dropping the remainder of the abstract; every `AbstractText` segment's full text is now reconstructed with `itertext`.
- arXiv connector now recognizes versioned new-style identifiers (for example `2301.00001v2`) as ids and resolves them via the `id_list` parameter. The previous `replace('.', '').isdigit()` check failed on the trailing `vN` suffix, so versioned ids were misrouted to a keyword `search_query` and returned search hits instead of the requested paper.
- Gemini provider adapter no longer raises `AttributeError` when the `candidates` list holds a non-object first element (for example a blocked/`null` candidate or a malformed gateway payload); it now guards `candidates[0]` like the OpenAI adapter and degrades to an empty completion instead of failing the request.
- OpenAI (and inherited Kimi) provider adapter now extracts text from a structured `message.content` part list returned by OpenAI-compatible gateways (LiteLLM, vLLM, OpenRouter) instead of coercing the list with `str(...)`, which previously emitted a Python repr (`[{'type': 'text', ...}]`) as the answer.
- Anthropic provider adapter now concatenates all `content` text blocks (skipping non-text blocks such as `thinking`/`tool_use`) instead of reading only `content[0].text`, which raised `KeyError` when a non-text block came first and truncated multi-block answers.
- `SafetyLimits.clamp_hops`/`clamp_sources` and `MultiHopRetriever` no longer apply hardcoded literal ceilings (`5`/`50`) alongside the configurable `max_hops`/`max_source_docs`/`max_depth`, which silently capped any limit raised above the historical defaults; the configured values are now the sole authoritative bounds.
- Gemini provider adapter now concatenates all `content.parts` text segments (skipping non-text parts) instead of reading only the first, preventing silent truncation of multi-part answers and dropped citations.
- Removed fabricated release notes and ADRs generated by automation.
- Replaced undeclared `requests` health check dependency with existing `httpx`.
- Removed Redis/distributed-mode references from docs and scripts.
- Repaired broken `scholar-rag-ingest` and `scholar-rag-eval` console scripts by packaging `scripts/`.
- Aligned CI and security scans with full `src/` mypy and bandit coverage.
